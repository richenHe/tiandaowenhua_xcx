# 天道文化 - 后台管理系统 API 文档

> **版本**: v1.0  
> **更新时间**: 2026-02-12  
> **技术栈**: CloudBase 云函数  
> **鉴权方式**: JWT Token（24小时有效）

---

## 📋 目录

- [1. 认证与鉴权](#1-认证与鉴权)
- [2. 用户管理](#2-用户管理)
- [3. 订单管理](#3-订单管理)
- [4. 课程管理](#4-课程管理)
- [5. 大使管理](#5-大使管理)
- [6. 系统管理](#6-系统管理)

---

## 🔐 通用说明

### 接口调用方式

所有云函数接口统一通过以下方式调用：

```javascript
// 云函数调用方式
wx.cloud.callFunction({
  name: '云函数名',  // 如: user, order, course, ambassador, system
  data: {
    action: 'admin:接口名',  // 如: admin:getUserList
    ...其他参数
  }
})
```

### 统一响应格式

```json
{
  "success": true,          // 成功标识
  "code": 0,                // 状态码（0=成功，其他=错误）
  "message": "操作成功",     // 提示信息
  "data": {...}             // 返回数据
}
```

### 状态码说明

| 状态码 | 说明 |
|-------|------|
| 0 | 成功 |
| 400 | 参数错误 |
| 401 | 未授权（未登录或token过期） |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 鉴权说明

除登录接口外，所有管理端接口均需要在请求头中携带 JWT Token：

```javascript
// HTTP 请求头
headers: {
  'Authorization': 'Bearer {token}'
}
```

---

## 1. 认证与鉴权

### 1.1 管理员登录

**接口**: `system.admin:login`  
**说明**: 管理员登录，返回 JWT Token  
**鉴权**: 否

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码（明文） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "admin": {
      "id": 1,
      "username": "admin",
      "real_name": "管理员",
      "role": "super_admin",
      "permissions": ["user:read", "order:write"]
    }
  }
}
```

#### 示例代码

```javascript
wx.cloud.callFunction({
  name: 'system',
  data: {
    action: 'admin:login',
    username: 'admin',
    password: '123456'
  }
}).then(res => {
  const { token, admin } = res.result.data;
  // 存储 token
  localStorage.setItem('admin_token', token);
});
```

---

## 2. 用户管理

### 2.1 获取学员列表

**接口**: `user.admin:getUserList`  
**说明**: 获取学员列表（支持搜索、筛选、分页）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| pageSize | Number | 否 | 每页数量（默认 20） |
| keyword | String | 否 | 关键词（姓名/手机号） |
| ambassadorLevel | Number | 否 | 大使等级筛选（0=普通用户，1-5=大使等级） |
| startDate | String | 否 | 注册开始日期（YYYY-MM-DD） |
| endDate | String | 否 | 注册结束日期（YYYY-MM-DD） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "total": 156,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "_openid": "oXXXX",
        "real_name": "张三",
        "phone": "138****8888",
        "city": "北京",
        "avatar": "https://...",
        "referee_code": "ABC123",
        "referee_id": 10,
        "ambassador_level": 1,
        "merit_points": 500,
        "cash_points": 1200.50,
        "frozen_cash_points": 0,
        "profile_completed": 1,
        "created_at": "2025-01-15T08:30:00.000Z"
      }
    ]
  }
}
```

---

### 2.2 获取学员详情

**接口**: `user.admin:getUserDetail`  
**说明**: 获取学员详细信息（包括订单、课程、积分等统计）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| userId | Number | 是 | 学员ID |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "real_name": "张三",
      "phone": "138****8888",
      "city": "北京",
      "avatar": "https://...",
      "referee_code": "ABC123",
      "ambassador_level": 1,
      "merit_points": 500,
      "cash_points_available": 1200.50,
      "cash_points_frozen": 0
    },
    "referee": {
      "id": 10,
      "real_name": "李四",
      "phone": "139****9999"
    },
    "refereeCount": 5,
    "courses": [],
    "orderStats": {
      "totalOrders": 3,
      "paidOrders": 3,
      "totalSpent": 5880.00
    },
    "meritPointsStats": {
      "totalEarned": 600,
      "totalSpent": 100,
      "balance": 500
    },
    "cashPointsStats": {
      "totalEarned": 1500.00,
      "totalWithdrawn": 299.50,
      "available": 1200.50,
      "frozen": 0
    },
    "withdrawStats": {
      "totalWithdrawals": 1,
      "approvedAmount": 299.50,
      "pendingAmount": 0
    }
  }
}
```

---

### 2.3 修改学员推荐人

**接口**: `user.admin:updateUserReferee`  
**说明**: 修改学员的推荐人  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| userId | Number | 是 | 学员ID |
| newRefereeId | Number | 是 | 新推荐人ID |
| reason | String | 是 | 修改原因 |

#### 响应数据

```json
{
  "success": true,
  "message": "推荐人修改成功",
  "data": {
    "userId": 1,
    "oldRefereeId": 10,
    "newRefereeId": 20,
    "changedAt": "2026-02-12T10:30:00.000Z"
  }
}
```

---

### 2.4 获取推荐人变更记录

**接口**: `user.admin:getRefereeChangeLogs`  
**说明**: 获取学员推荐人的变更记录  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| userId | Number | 否 | 学员ID（不传则查询所有） |
| page | Number | 否 | 页码（默认 1） |
| pageSize | Number | 否 | 每页数量（默认 20） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "total": 10,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "user_id": 1,
        "user_name": "张三",
        "old_referee_id": 10,
        "old_referee_name": "李四",
        "new_referee_id": 20,
        "new_referee_name": "王五",
        "reason": "用户申请更换",
        "admin_id": 1,
        "admin_name": "管理员",
        "created_at": "2026-02-12T10:30:00.000Z"
      }
    ]
  }
}
```

---

## 3. 订单管理

### 3.1 获取订单列表

**接口**: `order.admin:getOrderList`  
**说明**: 获取订单列表（支持筛选、搜索、分页）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| pay_status | Number | 否 | 支付状态（0=待支付，1=已支付，2=已取消，3=已关闭，4=已退款） |
| start_date | String | 否 | 开始日期（YYYY-MM-DD） |
| end_date | String | 否 | 结束日期（YYYY-MM-DD） |
| keyword | String | 否 | 关键词（订单号/用户姓名/手机号） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "total": 245,
    "page": 1,
    "page_size": 20,
    "list": [
      {
        "order_no": "2026021210305412345",
        "user_name": "张三",
        "user_phone": "138****8888",
        "order_type": 1,
        "order_name": "初探班",
        "final_amount": 1960.00,
        "pay_status": 1,
        "pay_status_name": "已支付",
        "pay_time": "2026-02-12T10:35:00.000Z",
        "referee_name": "李四",
        "is_reward_granted": 1,
        "created_at": "2026-02-12T10:30:00.000Z"
      }
    ]
  }
}
```

---

### 3.2 获取订单详情

**接口**: `order.admin:getOrderDetail`  
**说明**: 获取订单详细信息  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| order_no | String | 是 | 订单号 |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "order_no": "2026021210305412345",
    "user_id": 1,
    "user_name": "张三",
    "user_phone": "138****8888",
    "order_type": 1,
    "order_name": "初探班",
    "item_id": 1,
    "original_amount": 1980.00,
    "discount_amount": 20.00,
    "final_amount": 1960.00,
    "pay_status": 1,
    "pay_method": "wechat",
    "pay_time": "2026-02-12T10:35:00.000Z",
    "referee_id": 10,
    "referee_name": "李四",
    "referee_code": "LI001",
    "is_reward_granted": 1,
    "reward_granted_at": "2026-02-12T10:36:00.000Z",
    "created_at": "2026-02-12T10:30:00.000Z",
    "updated_at": "2026-02-12T10:36:00.000Z"
  }
}
```

---

### 3.3 订单退款

**接口**: `order.admin:refund`  
**说明**: 对已支付订单进行退款  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| order_no | String | 是 | 订单号 |
| refund_amount | Number | 是 | 退款金额 |
| refund_reason | String | 是 | 退款原因 |

#### 响应数据

```json
{
  "success": true,
  "message": "退款成功",
  "data": {
    "order_no": "2026021210305412345",
    "refund_amount": 1960.00,
    "refund_no": "R2026021211000012345",
    "refund_time": "2026-02-12T11:00:00.000Z"
  }
}
```

---

### 3.4 提现审核

**接口**: `order.admin:withdrawAudit`  
**说明**: 审核用户提现申请  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| withdrawal_id | Number | 是 | 提现记录ID |
| status | Number | 是 | 审核状态（1=通过，2=拒绝） |
| reject_reason | String | 否 | 拒绝原因（status=2时必填） |

#### 响应数据

```json
{
  "success": true,
  "message": "审核通过",
  "data": {
    "withdrawal_id": 123,
    "status": 1,
    "status_name": "审核通过"
  }
}
```

---

## 4. 课程管理

### 4.1 获取课程列表

**接口**: `course.admin:getCourseList`  
**说明**: 获取课程列表（支持筛选、搜索、分页）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 10） |
| type | Number | 否 | 课程类型（1=初探班，2=密训班，3=咨询服务） |
| status | Number | 否 | 状态（0=下架，1=上架） |
| keyword | String | 否 | 关键词（课程名称/描述） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "total": 15,
    "page": 1,
    "page_size": 10,
    "list": [
      {
        "id": 1,
        "name": "初探班",
        "type": 1,
        "type_name": "初探班",
        "cover_image": "https://...",
        "description": "课程简介",
        "content": "课程内容",
        "outline": "课程大纲",
        "teacher": "王老师",
        "duration": "2天1夜",
        "original_price": 1980.00,
        "current_price": 1960.00,
        "retrain_price": 980.00,
        "allow_retrain": 1,
        "stock": 100,
        "sold_count": 50,
        "sort_order": 1,
        "status": 1,
        "created_at": "2025-01-01T00:00:00.000Z",
        "updated_at": "2026-02-12T00:00:00.000Z"
      }
    ]
  }
}
```

---

### 4.2 创建课程

**接口**: `course.admin:createCourse`  
**说明**: 创建新课程  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| name | String | 是 | 课程名称 |
| type | Number | 是 | 课程类型（1=初探班，2=密训班，3=咨询服务） |
| current_price | Number | 是 | 当前价格 |
| cover_image | String | 否 | 封面图片 |
| description | String | 否 | 课程简介 |
| content | String | 否 | 课程内容 |
| outline | String | 否 | 课程大纲 |
| teacher | String | 否 | 讲师 |
| duration | String | 否 | 课程时长 |
| original_price | Number | 否 | 原价（默认等于current_price） |
| retrain_price | Number | 否 | 复训价格（默认0） |
| allow_retrain | Number | 否 | 是否允许复训（0=否，1=是） |
| sort_order | Number | 否 | 排序（默认0） |
| status | Number | 否 | 状态（0=下架，1=上架，默认1） |

#### 响应数据

```json
{
  "success": true,
  "message": "课程创建成功",
  "data": {
    "course_id": 16
  }
}
```

---

### 4.3 更新课程

**接口**: `course.admin:updateCourse`  
**说明**: 更新课程信息  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| id | Number | 是 | 课程ID |
| ...其他字段 | - | 否 | 与创建课程参数相同 |

---

### 4.4 删除课程

**接口**: `course.admin:deleteCourse`  
**说明**: 删除课程（逻辑删除）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| id | Number | 是 | 课程ID |

---

### 4.5 获取排期列表

**接口**: `course.admin:getClassRecordList`  
**说明**: 获取课程排期列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 10） |
| course_id | Number | 否 | 课程ID筛选 |
| status | Number | 否 | 状态筛选（0=未开始，1=进行中，2=已结束） |

---

### 4.6 创建排期

**接口**: `course.admin:createClassRecord`  
**说明**: 创建课程排期  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| course_id | Number | 是 | 课程ID |
| class_date | String | 是 | 上课日期（YYYY-MM-DD） |
| start_time | String | 是 | 开始时间（HH:mm） |
| end_time | String | 是 | 结束时间（HH:mm） |
| location | String | 是 | 上课地点 |
| max_participants | Number | 是 | 最大人数 |
| teacher | String | 否 | 讲师 |
| notes | String | 否 | 备注 |

---

### 4.7 更新排期

**接口**: `course.admin:updateClassRecord`  
**说明**: 更新排期信息  
**鉴权**: 是

---

### 4.8 删除排期

**接口**: `course.admin:deleteClassRecord`  
**说明**: 删除排期（逻辑删除）  
**鉴权**: 是

---

### 4.9 获取预约列表

**接口**: `course.admin:getAppointmentList`  
**说明**: 获取课程预约列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| course_id | Number | 否 | 课程ID筛选 |
| class_record_id | Number | 否 | 排期ID筛选 |
| status | Number | 否 | 状态筛选（0=待确认，1=已确认，2=已签到，3=已取消） |
| keyword | String | 否 | 关键词（用户姓名/手机号） |

---

### 4.10 更新预约状态

**接口**: `course.admin:updateAppointmentStatus`  
**说明**: 更新预约状态（确认/取消）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| appointment_id | Number | 是 | 预约ID |
| status | Number | 是 | 状态（1=已确认，3=已取消） |
| reason | String | 否 | 原因（取消时建议填写） |

---

### 4.11 批量签到

**接口**: `course.admin:batchCheckin`  
**说明**: 批量签到学员  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| appointment_ids | Array | 是 | 预约ID数组 |

#### 响应数据

```json
{
  "success": true,
  "message": "批量签到成功",
  "data": {
    "success_count": 5,
    "failed_count": 0,
    "failed_list": []
  }
}
```

---

### 4.12 获取案例列表

**接口**: `course.admin:getCaseList`  
**说明**: 获取学院案例列表  
**鉴权**: 是

---

### 4.13 创建案例

**接口**: `course.admin:createCase`  
**说明**: 创建学院案例  
**鉴权**: 是

---

### 4.14 更新案例

**接口**: `course.admin:updateCase`  
**说明**: 更新案例信息  
**鉴权**: 是

---

### 4.15 删除案例

**接口**: `course.admin:deleteCase`  
**说明**: 删除案例（逻辑删除）  
**鉴权**: 是

---

### 4.16 获取资料列表

**接口**: `course.admin:getMaterialList`  
**说明**: 获取学院资料列表  
**鉴权**: 是

---

### 4.17 创建资料

**接口**: `course.admin:createMaterial`  
**说明**: 创建学院资料  
**鉴权**: 是

---

### 4.18 更新资料

**接口**: `course.admin:updateMaterial`  
**说明**: 更新资料信息  
**鉴权**: 是

---

### 4.19 删除资料

**接口**: `course.admin:deleteMaterial`  
**说明**: 删除资料（逻辑删除）  
**鉴权**: 是

---

### 4.20 管理学院内容

**接口**: `course.admin:manageAcademyContent`  
**说明**: 管理学院章节和课程内容  
**鉴权**: 是

---

## 5. 大使管理

### 5.1 获取大使列表

**接口**: `ambassador.admin:getAmbassadorList`  
**说明**: 获取大使列表（支持筛选、搜索、分页）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| level | Number | 否 | 大使等级（1-5） |
| keyword | String | 否 | 关键词（姓名/手机号/推荐码） |

#### 响应数据

```json
{
  "success": true,
  "data": {
    "total": 50,
    "page": 1,
    "page_size": 20,
    "list": [
      {
        "id": 10,
        "uid": "U00010",
        "real_name": "李四",
        "phone": "139****9999",
        "avatar_url": "https://...",
        "referee_code": "LI001",
        "ambassador_level": 2,
        "merit_points": 1500,
        "cash_points": 3500.00,
        "referee_count": 12,
        "order_count": 25,
        "created_at": "2025-03-01T00:00:00.000Z",
        "ambassador_approved_at": "2025-03-05T00:00:00.000Z"
      }
    ]
  }
}
```

---

### 5.2 获取大使详情

**接口**: `ambassador.admin:getAmbassadorDetail`  
**说明**: 获取大使详细信息（包括业绩统计）  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| ambassador_id | Number | 是 | 大使ID |

---

### 5.3 获取申请列表

**接口**: `ambassador.admin:getApplicationList`  
**说明**: 获取大使申请列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| status | Number | 否 | 审核状态（0=待审核，1=已通过，2=已拒绝） |

---

### 5.4 审核申请

**接口**: `ambassador.admin:auditApplication`  
**说明**: 审核大使申请  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| application_id | Number | 是 | 申请ID |
| approved | Boolean | 是 | 是否通过（true=通过，false=拒绝） |
| reject_reason | String | 否 | 拒绝原因（approved=false时必填） |

#### 响应数据

```json
{
  "success": true,
  "message": "申请已通过，用户已成为初级大使",
  "data": {
    "application_id": 123,
    "approved": true
  }
}
```

---

### 5.5 获取活动列表

**接口**: `ambassador.admin:getActivityList`  
**说明**: 获取大使活动列表  
**鉴权**: 是

---

### 5.6 创建活动

**接口**: `ambassador.admin:createActivity`  
**说明**: 创建大使活动  
**鉴权**: 是

---

### 5.7 更新活动

**接口**: `ambassador.admin:updateActivity`  
**说明**: 更新活动信息  
**鉴权**: 是

---

### 5.8 删除活动

**接口**: `ambassador.admin:deleteActivity`  
**说明**: 删除活动（逻辑删除）  
**鉴权**: 是

---

### 5.9 获取合约模板列表

**接口**: `ambassador.admin:getContractTemplateList`  
**说明**: 获取合约模板列表  
**鉴权**: 是

---

### 5.10 创建合约模板

**接口**: `ambassador.admin:createContractTemplate`  
**说明**: 创建合约模板  
**鉴权**: 是

---

### 5.11 更新合约模板

**接口**: `ambassador.admin:updateContractTemplate`  
**说明**: 更新合约模板  
**鉴权**: 是

---

### 5.12 删除合约模板

**接口**: `ambassador.admin:deleteContractTemplate`  
**说明**: 删除合约模板（逻辑删除）  
**鉴权**: 是

---

### 5.13 获取签约列表

**接口**: `ambassador.admin:getSignatureList`  
**说明**: 获取大使签约列表  
**鉴权**: 是

---

### 5.14 获取合约版本

**接口**: `ambassador.admin:getContractVersions`  
**说明**: 获取某个模板的所有版本  
**鉴权**: 是

---

### 5.15 获取即将到期合约

**接口**: `ambassador.admin:getExpiringContracts`  
**说明**: 获取即将到期的合约列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| days | Number | 否 | 提前天数（默认30天） |

---

## 6. 系统管理

### 6.1 获取统计数据

**接口**: `system.admin:getStatistics`  
**说明**: 获取系统统计数据（仪表盘）  
**鉴权**: 是

#### 响应数据

```json
{
  "success": true,
  "data": {
    "users": {
      "total": 1560,
      "today": 12,
      "ambassadors": 50
    },
    "orders": {
      "total": 856,
      "today": 5,
      "total_amount": "568900.00"
    },
    "courses": {
      "total_appointments": 320,
      "checked_in": 280,
      "checkin_rate": "87.50"
    },
    "ambassadors": {
      "total": 50,
      "level_distribution": {
        "level_1": 30,
        "level_2": 12,
        "level_3": 5,
        "level_4": 2,
        "level_5": 1
      }
    },
    "today": {
      "users": 12,
      "orders": 5,
      "appointments": 8,
      "feedbacks": 3
    }
  }
}
```

---

### 6.2 获取管理员列表

**接口**: `system.admin:getAdminUserList`  
**说明**: 获取管理员列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| status | Number | 否 | 状态筛选（0=禁用，1=启用） |

---

### 6.3 创建管理员

**接口**: `system.admin:createAdminUser`  
**说明**: 创建管理员账号  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |
| real_name | String | 是 | 真实姓名 |
| role | String | 是 | 角色（super_admin/admin/operator） |
| permissions | Array | 否 | 权限列表 |
| status | Number | 否 | 状态（0=禁用，1=启用，默认1） |

---

### 6.4 更新管理员

**接口**: `system.admin:updateAdminUser`  
**说明**: 更新管理员信息  
**鉴权**: 是

---

### 6.5 删除管理员

**接口**: `system.admin:deleteAdminUser`  
**说明**: 删除管理员（逻辑删除）  
**鉴权**: 是

---

### 6.6 获取系统配置

**接口**: `system.admin:getConfig`  
**说明**: 获取系统配置  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| config_key | String | 否 | 配置键（不传则返回所有） |

---

### 6.7 更新系统配置

**接口**: `system.admin:updateConfig`  
**说明**: 更新系统配置  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| config_key | String | 是 | 配置键 |
| config_value | String | 是 | 配置值 |

---

### 6.8 获取公告列表

**接口**: `system.admin:getAnnouncementList`  
**说明**: 获取公告列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| status | Number | 否 | 状态筛选（0=草稿，1=发布） |

---

### 6.9 创建公告

**接口**: `system.admin:createAnnouncement`  
**说明**: 创建系统公告  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| title | String | 是 | 公告标题 |
| content | String | 是 | 公告内容 |
| type | Number | 是 | 公告类型（1=系统通知，2=活动公告，3=更新日志） |
| status | Number | 否 | 状态（0=草稿，1=发布，默认1） |
| publish_time | String | 否 | 发布时间（不传则立即发布） |

---

### 6.10 更新公告

**接口**: `system.admin:updateAnnouncement`  
**说明**: 更新公告信息  
**鉴权**: 是

---

### 6.11 删除公告

**接口**: `system.admin:deleteAnnouncement`  
**说明**: 删除公告（逻辑删除）  
**鉴权**: 是

---

### 6.12 获取反馈列表

**接口**: `system.admin:getFeedbackList`  
**说明**: 获取用户反馈列表  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| type | Number | 否 | 反馈类型筛选 |
| status | Number | 否 | 处理状态（0=待处理，1=已回复，2=已关闭） |

---

### 6.13 回复反馈

**接口**: `system.admin:replyFeedback`  
**说明**: 回复用户反馈  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| feedback_id | Number | 是 | 反馈ID |
| reply_content | String | 是 | 回复内容 |
| status | Number | 否 | 处理状态（1=已回复，2=已关闭） |

---

### 6.14 获取通知配置列表

**接口**: `system.admin:getNotificationConfigList`  
**说明**: 获取订阅消息配置列表  
**鉴权**: 是

---

### 6.15 创建通知配置

**接口**: `system.admin:createNotificationConfig`  
**说明**: 创建订阅消息配置  
**鉴权**: 是

---

### 6.16 更新通知配置

**接口**: `system.admin:updateNotificationConfig`  
**说明**: 更新订阅消息配置  
**鉴权**: 是

---

### 6.17 获取通知日志

**接口**: `system.admin:getNotificationLogs`  
**说明**: 获取订阅消息发送日志  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| page | Number | 否 | 页码（默认 1） |
| page_size | Number | 否 | 每页数量（默认 20） |
| start_date | String | 否 | 开始日期 |
| end_date | String | 否 | 结束日期 |
| status | Number | 否 | 发送状态（0=失败，1=成功） |

---

### 6.18 发送通知

**接口**: `system.admin:sendNotification`  
**说明**: 手动发送订阅消息  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| user_ids | Array | 是 | 用户ID数组（支持批量） |
| template_id | String | 是 | 模板ID |
| data | Object | 是 | 模板数据 |
| page | String | 否 | 跳转页面 |

---

### 6.19 获取大使等级配置

**接口**: `system.admin:getAmbassadorLevelConfigs`  
**说明**: 获取大使等级配置  
**鉴权**: 是

---

### 6.20 初始化大使等级配置

**接口**: `system.admin:initAmbassadorLevelConfigs`  
**说明**: 初始化大使等级配置（首次安装使用）  
**鉴权**: 是

---

### 6.21 更新大使等级配置

**接口**: `system.admin:updateAmbassadorLevelConfig`  
**说明**: 更新某个等级的配置  
**鉴权**: 是

#### 请求参数

| 参数 | 类型 | 必填 | 说明 |
|-----|------|-----|------|
| level | Number | 是 | 大使等级（1-5） |
| level_name | String | 否 | 等级名称 |
| merit_reward | Number | 否 | 功勋奖励 |
| cash_reward | Number | 否 | 现金奖励 |
| referee_requirement | Number | 否 | 推荐人数要求 |
| order_requirement | Number | 否 | 订单数量要求 |
| description | String | 否 | 等级描述 |

---

## 📌 附录

### 云函数模块说明

| 云函数名 | 模块 | 说明 |
|---------|------|------|
| user | 用户管理 | 学员信息、推荐关系、积分管理 |
| order | 订单管理 | 订单列表、订单详情、退款、提现审核 |
| course | 课程管理 | 课程、排期、预约、签到、案例、资料 |
| ambassador | 大使管理 | 大使列表、申请审核、活动、合约 |
| system | 系统管理 | 统计、管理员、配置、公告、反馈、通知 |

### 数据库表说明

| 表名 | 说明 |
|-----|------|
| users | 用户表 |
| orders | 订单表 |
| courses | 课程表 |
| class_records | 排期表 |
| appointments | 预约表 |
| withdrawals | 提现记录表 |
| ambassador_applications | 大使申请表 |
| ambassador_activities | 大使活动表 |
| contracts | 合约表 |
| admin_users | 管理员表 |
| system_configs | 系统配置表 |
| announcements | 公告表 |
| feedbacks | 反馈表 |
| notification_logs | 通知日志表 |

### 常用枚举值

#### 订单状态（pay_status）
- `0`: 待支付
- `1`: 已支付
- `2`: 已取消
- `3`: 已关闭
- `4`: 已退款

#### 课程类型（type）
- `1`: 初探班
- `2`: 密训班
- `3`: 咨询服务

#### 预约状态（status）
- `0`: 待确认
- `1`: 已确认
- `2`: 已签到
- `3`: 已取消

#### 大使等级（ambassador_level）
- `0`: 普通用户
- `1`: 初级大使
- `2`: 中级大使
- `3`: 高级大使
- `4`: 资深大使
- `5`: 特级大使

#### 申请状态（status）
- `0`: 待审核
- `1`: 已通过
- `2`: 已拒绝

#### 管理员角色（role）
- `super_admin`: 超级管理员
- `admin`: 管理员
- `operator`: 运营人员

---

## 📝 更新日志

### v1.0 (2026-02-12)
- ✅ 初始版本
- ✅ 完成所有后台管理接口文档
- ✅ 包含6大模块：认证、用户、订单、课程、大使、系统
- ✅ 共计 80+ 个管理端接口

---

## 📮 联系方式

如有问题，请联系技术团队。

**文档维护**: 天道文化技术团队  
**最后更新**: 2026-02-12





