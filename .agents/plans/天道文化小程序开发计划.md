# 天道文化小程序开发计划

> 基于 prototype-tdesign 原型图的 UniApp + TDesign CSS 开发计划

---

## 一、项目概述

### 1.1 功能描述

**天道文化小程序**是一个基于孙膑道天道文化课程的线上学习、推广和管理平台。主要功能包括：

- **课程学习**：浏览、购买、学习天道文化课程
- **预约系统**：预约线下课程和咨询
- **大使推广**：传播大使制度，支持分销推广
- **个人中心**：订单管理、预约管理、个人资料
- **商学院**：商学院介绍、学员案例、素材库

### 1.2 技术方案

| 技术 | 说明 |
|-----|------|
| **框架** | UniApp + Vue 3 Composition API |
| **样式** | TDesign CSS 组件系统（`.t-` 类名前缀） |
| **样式变量** | `--td-*` 前缀的 CSS 自定义属性 |
| **适配** | 响应式设计，自适应不同手机屏幕 |
| **后端** | 腾讯云开发 CloudBase |
| **登录** | 已实现：微信登录、手机号、邮箱、密码（见 cloudbase.ts） |
| **支付** | 仅支持微信支付 |

### 1.3 样式系统说明

**原型图样式来源**：`prototype-tdesign/styles/` 和 `prototype-tdesign/components/`

| 类型 | 文件路径 | 说明 |
|-----|---------|------|
| 主题变量 | `prototype-tdesign/styles/tdesign-theme.css` | `--td-brand-color` 等变量定义 |
| 组件样式 | `prototype-tdesign/components/*.css` | `.t-button`, `.t-card` 等组件样式 |
| 项目变量 | `src/styles/tdesign-vars.scss` | SCSS 变量版本，供小程序使用 |
| 全局样式 | `src/styles/common.scss` | 全局公共样式 |

---

## 二、页面清单与开发优先级

### 2.1 页面总览（共34+页面）

| 模块 | 页面数 | 优先级 | 预估工作量 |
|-----|-------|-------|-----------|
| 授权登录 | 2页 | P0（基础） | 2人天 |
| 首页 | 1页 | P0（核心） | 1.5人天 |
| 课程模块 | 4页 | P0（核心） | 4人天 |
| 订单模块 | 4页 | P0（核心） | 3人天 |
| 个人中心 | 8页 | P1（重要） | 5人天 |
| 大使系统 | 11页 | P1（重要） | 8人天 |
| 商学院 | 3页 | P2（一般） | 2人天 |
| 商城 | 1页 | P2（一般） | 1人天 |
| 公共页面 | 1页 | P2（一般） | 0.5人天 |

**总计：约 27 人天**

### 2.2 详细页面列表

#### 授权模块 (pages/auth/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 登录 | `login/index.vue` | 微信一键登录入口 | P0 |
| 完善资料 | `complete-profile/index.vue` | 新用户首次登录完善信息 | P0 |

#### 首页模块 (pages/index/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 首页 | `index.vue` | Banner轮播、课程分类、课程列表 | P0 |

#### 课程模块 (pages/course/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 课程详情 | `detail/index.vue` | 课程介绍、立即购买按钮 | P0 |
| 我的课程 | `my-courses/index.vue` | 已购课程列表 | P0 |
| 课程计划 | `schedule/index.vue` | 线下课程排期、预约 | P0 |
| 预约确认 | `appointment-confirm/index.vue` | 预约信息确认 | P1 |

#### 订单模块 (pages/order/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 订单确认 | `confirm/index.vue` | 订单信息确认、选择推荐人 | P0 |
| 支付页 | `payment/index.vue` | 微信支付 | P0 |
| 订单详情 | `detail/index.vue` | 订单详细信息 | P0 |
| 选择推荐人 | `select-referee/index.vue` | 选择/更换推荐人 | P1 |

#### 个人中心 (pages/mine/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 个人中心首页 | `index.vue` | 用户信息、数据统计、菜单入口 | P0 |
| 个人资料 | `profile/index.vue` | 编辑个人资料 | P0 |
| 我的订单 | `orders/index.vue` | 订单列表 | P0 |
| 我的预约 | `appointments/index.vue` | 预约列表 | P0 |
| 我的合同 | `contracts/index.vue` | 合同列表 | P1 |
| 意见反馈 | `feedback/index.vue` | 提交反馈 | P1 |
| 在线咨询 | `consultation/index.vue` | 联系客服 | P2 |
| 引荐人列表 | `referral-list/index.vue` | 我推荐的学员列表 | P1 |
| 推荐人管理 | `referee-manage/index.vue` | 管理我的推荐人 | P1 |

#### 大使系统 (pages/ambassador/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 大使等级 | `level/index.vue` | 查看等级、功德分、积分 | P0 |
| 申请大使 | `apply/index.vue` | 提交大使申请 | P0 |
| 我的团队 | `team/index.vue` | 下级大使列表 | P1 |
| 推广二维码 | `qrcode/index.vue` | 推广二维码 | P1 |
| 功德分 | `merit-points/index.vue` | 功德分记录 | P1 |
| 可提现积分 | `cash-points/index.vue` | 积分明细、提现 | P1 |
| 升级指南 | `upgrade-guide/index.vue` | 升级说明 | P2 |
| 活动记录 | `activity-records/index.vue` | 推广活动记录 | P2 |
| 合同详情 | `contract-detail/index.vue` | 查看合同 | P1 |
| 合同签署 | `contract-sign/index.vue` | 电子签章 | P1 |
| 提现 | `withdraw/index.vue` | 申请提现 | P1 |

#### 商学院 (pages/academy/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 商学院介绍 | `intro/index.vue` | 商学院简介、师资、历程 | P2 |
| 学员案例 | `cases/index.vue` | 成功案例展示 | P2 |
| 朋友圈素材 | `materials/index.vue` | 海报、文案素材 | P2 |

#### 商城 (pages/mall/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 商城首页 | `index.vue` | 商品展示 | P2 |

#### 公共页面 (pages/common/)

| 页面 | 文件 | 功能描述 | 优先级 |
|-----|-----|---------|-------|
| 平台公告 | `announcement/index.vue` | 公告列表、详情 | P1 |

---

## 三、样式系统设计

### 3.1 TDesign CSS 组件系统

**样式来源**：从 `prototype-tdesign/` 复制组件 CSS 到项目

**核心类名前缀**：`.t-` （如 `.t-button`, `.t-card`, `.t-form-item`）

### 3.2 组件样式清单

| 组件 | 原型CSS文件 | 项目文件 | 说明 |
|-----|------------|---------|------|
| 按钮 | `prototype-tdesign/components/button.css` | `src/styles/components/button.scss` | `.t-button`, `.t-button--theme-*` |
| 卡片 | `prototype-tdesign/components/card.css` | `src/styles/components/card.scss` | `.t-card`, `.t-card__body` |
| 输入框 | `prototype-tdesign/components/input.css` | `src/styles/components/input.scss` | `.t-input__wrap`, `.t-input__inner` |
| 表单 | `prototype-tdesign/components/form.css` | `src/styles/components/form.scss` | `.t-form-item`, `.t-form-item__label` |
| 单选框 | `prototype-tdesign/components/radio.css` | `src/styles/components/radio.scss` | `.t-radio`, `.t-radio__input` |
| 选择器 | `prototype-tdesign/components/select.css` | `src/styles/components/select.scss` | `.t-select`, `.t-select__input` |
| 标签页 | `prototype-tdesign/components/tabs.css` | `src/styles/components/tabs.scss` | `.t-tabs`, `.t-tabs__item` |
| 徽章 | `prototype-tdesign/components/badge.css` | `src/styles/components/badge.scss` | `.t-badge`, `.t-badge--*` |
| 头像 | `prototype-tdesign/components/avatar.css` | `src/styles/components/avatar.scss` | `.t-avatar`, `.t-avatar--*` |
| 对话框 | `prototype-tdesign/components/dialog.css` | `src/styles/components/dialog.scss` | `.t-dialog`, `.t-dialog__content` |
| 分割线 | `prototype-tdesign/components/divider.css` | `src/styles/components/divider.scss` | `.t-divider` |
| 返回按钮 | `prototype-tdesign/components/back-button.css` | `src/styles/components/back-button.scss` | `.back-button` |
| 提示框 | `prototype-tdesign/components/alert.css` | `src/styles/components/alert.scss` | `.t-alert`, `.t-alert--theme-*` |
| 进度条 | `prototype-tdesign/components/progress.css` | `src/styles/components/progress.scss` | `.t-progress`, `.t-progress__bar` |

### 3.3 TDesign 颜色变量

```scss
// src/styles/tdesign-vars.scss

// ========== 颜色系统 ==========
$td-brand-color: #0052D9;           // 主品牌色
$td-brand-color-light: #266FE8;     // 品牌色-浅
$td-brand-color-lighter: #4787E8;
$td-brand-color-dark: #003BA8;

// 背景色
$td-bg-color-page: #F5F5F5;         // 页面背景
$td-bg-color-container: #FFFFFF;    // 容器背景(卡片)
$td-bg-color-container-hover: #FAFAFA;
$td-bg-color-container-active: #F5F5F5;
$td-bg-color-container-select: #F3F6FF; // 容器选中背景

// 文本颜色
$td-text-color-primary: #000000;    // 主文本
$td-text-color-secondary: #5E5E5E;  // 次要文本
$td-text-color-placeholder: #BBBBBB; // 占位文本
$td-text-color-disabled: #DDDDDD;   // 禁用文本
$td-text-color-brand: #0052D9;      // 品牌色文本

// 边框颜色
$td-border-level-0: #F0F0F0;
$td-border-level-1: #E7E7E7;
$td-border-level-2: #EEEEEE;

// 功能色
$td-success-color: #00A870;
$td-warning-color: #D4AF37;         // 金色(价格、徽章)
$td-error-color: #E34D59;
$td-info-color: #0052D9;

// ========== 间距系统 ==========
$td-comp-paddingTB-xxs: 1px;
$td-comp-paddingTB-xs: 2px;
$td-comp-paddingTB-s: 4px;
$td-comp-paddingTB-m: 8px;
$td-comp-paddingTB-l: 12px;
$td-comp-paddingTB-xl: 16px;
$td-comp-paddingTB-xxl: 24px;

$td-comp-paddingLR-xxs: 4px;
$td-comp-paddingLR-xs: 8px;
$td-comp-paddingLR-s: 12px;
$td-comp-paddingLR-m: 16px;
$td-comp-paddingLR-l: 24px;
$td-comp-paddingLR-xl: 32px;
$td-comp-paddingLR-xxl: 40px;

$td-page-margin: 16px;              // 页面边距
$td-card-spacing: 12px;             // 卡片间距
$td-section-spacing: 24px;          // 区块间距

// ========== 圆角系统 ==========
$td-radius-small: 3px;
$td-radius-default: 6px;            // 默认圆角
$td-radius-medium: 9px;
$td-radius-large: 12px;             // 大圆角
$td-radius-extraLarge: 16px;
$td-radius-round: 999px;            // 完全圆形
$td-radius-circle: 50%;

// ========== 阴影系统 ==========
$td-shadow-1: 0 1px 10px rgba(0, 0, 0, 0.05);
$td-shadow-2: 0 2px 20px rgba(0, 0, 0, 0.08);
$td-shadow-3: 0 6px 30px rgba(0, 0, 0, 0.12);

// ========== 字体系统 ==========
$td-font-size-s: 12px;
$td-font-size-base: 14px;
$td-font-size-m: 16px;
$td-font-size-l: 18px;
$td-font-size-xl: 20px;
$td-font-size-xxl: 24px;

$td-font-weight-regular: 400;
$td-font-weight-medium: 500;
$td-font-weight-semibold: 600;
$td-font-weight-bold: 700;
```

### 3.4 全局公共样式

```scss
// src/styles/common.scss

/* 页面容器 */
.page-container {
  min-height: 100vh;
  background: $td-bg-color-page;
  display: flex;
  flex-direction: column;
  padding-top: 88rpx; /* 状态栏高度 */
}

/* 页面内容 */
.page-content {
  padding: $td-page-margin;
}

/* 滚动区域 */
.scroll-area {
  flex: 1;
  overflow: hidden;
}

.scroll-area--with-header-footer {
  padding-bottom: 200rpx;
}

/* 固定底部 */
.fixed-bottom {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: $td-bg-color-container;
  padding: $td-comp-paddingTB-xxl $td-page-margin;
  padding-bottom: calc(#{$td-comp-paddingTB-xxl} + env(safe-area-inset-bottom));
  box-shadow: $td-shadow-1;
  box-sizing: border-box;
}

/* 设备-safe-area */
.device-safe-area-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 3.5 响应式设计

```scss
// 响应式断点
$breakpoint-sm: 320px;
$breakpoint-md: 375px;
$breakpoint-lg: 414px;

// 页面容器响应式
.page-container {
  width: 100%;
  max-width: 100%;
  margin: 0 auto;

  @media (min-width: $breakpoint-lg) {
    max-width: 414px; // 最大宽度适配大屏手机
  }
}
```

---

## 四、组件开发计划

### 4.1 基础布局组件

| 组件 | 路径 | 功能 | 说明 |
|-----|-----|-----|------|
| 页面容器 | `components/PageContainer.vue` | 页面布局、滚动区域 | 响应式适配 |
| 页面头部 | `components/PageHeader.vue` | 标题、返回按钮 | 响应式 |
| 底部导航 | `components/TabBar.vue` | 底部TabBar | 高适配性 |

### 4.2 通用组件

| 组件 | 路径 | 功能 | TDesign类 |
|-----|-----|-----|-----------|
| 卡片 | `components/Card.vue` | 通用卡片样式 | `.t-card`, `.t-card__body` |
| 按钮 | `components/Button.vue` | 主题按钮 | `.t-button`, `.t-button--theme-primary` |
| 头像 | `components/Avatar.vue` | 用户头像 | `.t-avatar`, `.t-avatar--circle` |
| 徽章 | `components/Badge.vue` | 状态徽章 | `.t-badge`, `.t-badge--warning` |
| 列表项 | `components/ListItem.vue` | 列表条目 | 自定义 `.list-item` 样式 |
| 分割线 | `components/Divider.vue` | 分割线 | `.t-divider` |

### 4.3 业务组件

| 组件 | 路径 | 功能 |
|-----|-----|-----|
| 轮播图 | `components/Banner.vue` | 首页Banner |
| 通知条 | `components/NoticeBar.vue` | 滚动通知 |
| 课程卡片 | `components/CourseCard.vue` | 课程列表项 |
| 订单卡片 | `components/OrderCard.vue` | 订单列表项 |
| 统计卡片 | `components/StatCard.vue` | 数据统计 |
| 大使等级卡 | `components/AmbassadorLevel.vue` | 大使等级展示 |

---

## 五、API 服务层设计

### 5.1 已有的登录 API（cloudbase.ts）

```typescript
import {
  signInWithOpenId,      // 微信静默登录
  signInWithPhoneAuth,   // 手机号一键登录
  signInWithPhoneCode,   // 手机验证码登录
  signInWithPassword,    // 密码登录
  signInWithEmailCode,   // 邮箱验证码登录
} from '@/utils/cloudbase';
```

### 5.2 API 目录结构

```
src/api/
├── index.ts          # API 统一导出
├── auth.ts           # 认证相关（复用 cloudbase.ts）
├── user.ts           # 用户相关
├── course.ts         # 课程相关
├── order.ts          # 订单相关
├── appointment.ts    # 预约相关
├── ambassador.ts     # 大使相关
└── announcement.ts   # 公告相关
```

### 5.3 数据模型

```typescript
// src/models/index.ts

export interface User {
  id: string;
  nickname: string;
  avatar: string;
  phone: string;
  isAmbassador: boolean;
  ambassadorLevel: AmbassadorLevel;
  refereeId?: string;
}

export interface Course {
  id: string;
  title: string;
  description: string;
  price: number;
  type: '初探班' | '密训班';
  image?: string;
}

export interface Order {
  id: string;
  courseId: string;
  status: 'pending' | 'paid' | 'cancelled';
  refereeId?: string;
  amount: number;
  createTime: Date;
  payTime?: Date;
}

export interface Ambassador {
  level: 'none' | '准青鸾' | '青鸾' | '鸿鹄' | '金凤';
  meritPoints: number;
  cashPoints: number;
  contractStatus: 'none' | 'pending' | 'signed';
}

export interface Appointment {
  id: string;
  courseName: string;
  date: string;
  location: string;
  status: 'pending' | 'confirmed' | 'cancelled';
}
```

---

## 六、页面开发详细说明

### 6.1 登录页 (pages/auth/login/index.vue)

**参考原型**：`prototype-tdesign/pages/auth/login.html`

```vue
<template>
  <view class="page-container min-h-screen bg-gradient-to-br from-blue-500 to-purple-600">
    <view class="flex flex-col items-center justify-center p-4" style="height: 100vh;">
      <!-- Logo -->
      <view class="w-20 h-20 rounded-lg bg-brand flex items-center justify-center mb-6 shadow-lg">
        <text class="text-white text-3xl">🏛️</text>
      </view>

      <!-- 标题 -->
      <text class="text-xl font-semibold text-primary mb-2">天道文化课程平台</text>
      <text class="text-sm text-secondary mb-10">传承国学智慧 · 弘扬天道文化</text>

      <!-- 登录卡片 -->
      <view class="bg-container rounded-lg shadow-lg p-6 w-full" style="max-width: 340px;">
        <button
          class="w-full rounded bg-brand text-white py-3 flex items-center justify-center gap-2"
          @click="handleWechatLogin"
        >
          <text>🔐</text>
          <text class="font-medium">微信一键登录</text>
        </button>

        <text class="text-xs text-placeholder text-center mt-4 block">
          登录即代表同意《用户协议》和《隐私政策》
        </text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { signInWithOpenId } from '@/utils/cloudbase';

const handleWechatLogin = async () => {
  try {
    await signInWithOpenId();
    uni.navigateTo({ url: '/pages/index/index' });
  } catch (error) {
    uni.showToast({ title: '登录失败', icon: 'none' });
  }
};
</script>
```

### 6.2 首页 (pages/index/index.vue)

**参考原型**：`prototype-tdesign/pages/index/home.html`

```vue
<template>
  <view class="page-container min-h-screen bg-page">
    <!-- 通知铃铛 -->
    <view class="absolute top-4 right-4 z-10" @click="goToAnnouncement">
      <text class="text-2xl">🔔</text>
    </view>

    <!-- 滚动区域 -->
    <scroll-view scroll-y class="h-screen pb-16">
      <!-- Banner 轮播 -->
      <swiper class="w-full h-70" indicator-dots autoplay circular indicator-color="rgba(255,255,255,0.5)" indicator-active-color="#fff">
        <swiper-item v-for="(item, index) in banners" :key="index">
          <view class="w-full h-full flex flex-col items-center justify-center text-white p-8" :class="item.bgClass">
            <text class="text-2xl font-semibold mb-2">{{ item.title }}</text>
            <text class="text-sm text-center opacity-95">{{ item.subtitle }}</text>
          </view>
        </swiper-item>
      </swiper>

      <!-- 通知条 -->
      <view class="bg-info-light px-4 py-2 flex items-center gap-2" v-if="notice">
        <text>📢</text>
        <text class="text-brand text-sm flex-1 truncate">{{ notice }}</text>
        <text class="text-placeholder">›</text>
      </view>

      <!-- 课程分类 Tab -->
      <view class="flex border-b border-page bg-container">
        <view v-for="(tab, index) in courseTabs" :key="index" class="flex-1 py-3 text-center"
          :class="activeTab === index ? 'text-brand border-b-2 border-brand font-medium' : 'text-secondary'"
          @click="activeTab = index">
          {{ tab }}
        </view>
      </view>

      <!-- 课程列表 -->
      <view class="p-4">
        <view v-for="course in courseList" :key="course.id" class="bg-container rounded mb-4 shadow overflow-hidden"
          @click="goToCourseDetail(course)">
          <view class="h-40 bg-gradient-to-r from-pink-400 to-red-500 flex items-center justify-center">
            <text class="text-5xl">{{ course.icon }}</text>
          </view>
          <view class="p-4">
            <view class="flex justify-between items-center mb-2">
              <text class="text-lg font-semibold">{{ course.title }}</text>
              <text v-if="course.purchased" class="bg-success text-white text-xs px-2 py-0.5 rounded">已购买</text>
            </view>
            <text class="text-warning text-xl font-semibold">¥{{ course.price }}</text>
          </view>
        </view>
      </view>
    </scroll-view>

    <TabBar :active="0" />
  </view>
</template>
```

### 6.3 个人中心 (pages/mine/index.vue)

**参考原型**：`prototype-tdesign/pages/mine/index.html`

```vue
<template>
  <view class="page-container min-h-screen bg-page">
    <scroll-view scroll-y class="h-screen pb-16">
      <!-- 用户头部 -->
      <view class="bg-gradient-to-r from-brand to-brand-light p-6 text-white">
        <view class="flex items-center gap-4" @click="goToProfile">
          <view class="w-16 h-16 rounded-full bg-white flex items-center justify-center">
            <text class="text-2xl text-brand font-semibold">{{ userInfo.nickname?.charAt(0) }}</text>
          </view>
          <view class="flex-1">
            <view class="flex items-center gap-2">
              <text class="text-xl font-semibold">{{ userInfo.nickname }}</text>
              <text>{{ userInfo.levelIcon }}</text>
            </view>
            <text class="text-sm opacity-90">{{ userInfo.phone }}</text>
          </view>
          <text class="opacity-80">›</text>
        </view>
      </view>

      <!-- 数据统计 -->
      <view class="bg-container grid grid-cols-4 py-4 border-b">
        <view v-for="(stat, index) in stats" :key="index" class="flex flex-col items-center" @click="stat.onClick">
          <text class="text-xl font-semibold" :class="stat.colorClass">{{ stat.value }}</text>
          <text class="text-xs text-secondary mt-1">{{ stat.label }}</text>
        </view>
      </view>

      <!-- 菜单列表 -->
      <view class="bg-container mt-3">
        <view class="px-4 py-3 text-secondary text-sm">⚙️ 推荐与设置</view>
        <view v-for="(menu, index) in menus1" :key="index"
          class="flex justify-between items-center px-4 py-4 border-t border-page" @click="goToPage(menu.url)">
          <view class="flex items-center gap-3">
            <text class="text-xl">{{ menu.icon }}</text>
            <text>{{ menu.title }}</text>
          </view>
          <view class="flex items-center gap-2">
            <text v-if="menu.badge" class="bg-brand text-white text-xs px-2 py-0.5 rounded">{{ menu.badge }}</text>
            <text class="text-placeholder">›</text>
          </view>
        </view>
      </view>

      <view class="bg-container mt-3">
        <view class="px-4 py-3 text-secondary text-sm">💬 帮助与反馈</view>
        <view v-for="(menu, index) in menus2" :key="index"
          class="flex justify-between items-center px-4 py-4 border-t border-page" @click="goToPage(menu.url)">
          <view class="flex items-center gap-3">
            <text class="text-xl">{{ menu.icon }}</text>
            <text>{{ menu.title }}</text>
          </view>
          <text class="text-placeholder">›</text>
        </view>
      </view>
    </scroll-view>

    <TabBar :active="3" />
  </view>
</template>
```

---

## 七、开发阶段规划

### 阶段一：基础架构（2人天）

| 任务 | 内容 | 产出 | 验收 |
|-----|-----|-----|-----|
| 样式系统 | 创建 tailwind.scss 全局样式 | 样式文件 | 编译通过 |
| 基础组件 | PageContainer, PageHeader, TabBar | 3个组件 | 组件可用 |
| 路由配置 | 配置 pages.json | 路由完成 | 页面可访问 |

**阶段一验收：**
- [ ] 项目能正常编译 `npm run dev:mp-weixin`
- [ ] 微信开发者工具能正常预览
- [ ] TabBar 显示正常

### 阶段二：授权登录（1人天）

| 任务 | 内容 | 产出 | 验收 |
|-----|-----|-----|-----|
| 登录页 | 微信一键登录（signInWithOpenId） | login/index.vue | 样式+功能验证 |
| 完善资料 | 首次登录信息完善 | complete-profile/index.vue | 样式+功能验证 |

**阶段二验收清单：**

**登录页：**
- [ ] 背景为纯白色
- [ ] 按钮为浅蓝色填充（文字蓝色 #0052D9，背景 #F3F6FF）
- [ ] Logo、标题、协议文字显示正确
- [ ] 点击登录按钮能正常跳转

**完善资料页：**
- [ ] 无系统导航栏（已配置 `navigationStyle: custom`）
- [ ] 自定义头部不被状态栏遮挡
- [ ] 提示框、表单、按钮样式正确
- [ ] 提交功能正常

### 阶段三：核心页面（5人天）

| 任务 | 时间 | 产出 | 验收 |
|-----|-----|-----|-----|
| 首页 Banner + 课程列表 | 1.5天 | index/index.vue | 样式+功能验证 |
| 课程详情 + 我的课程 | 1.5天 | course/detail, my-courses | 样式+功能验证 |
| 订单确认 + 支付 + 详情 | 2天 | order/* | 样式+功能验证 |

### 阶段四：个人中心（3人天）

| 任务 | 时间 | 产出 | 验收 |
|-----|-----|-----|-----|
| 个人中心首页 | 1天 | mine/index.vue | 样式+功能验证 |
| 个人资料、订单、预约 | 1.5天 | mine/profile, orders, appointments | 样式+功能验证 |
| 推荐人、引荐人列表 | 0.5天 | mine/referee-manage, referral-list | 样式+功能验证 |

### 阶段五：大使系统（5人天）

| 任务 | 时间 | 产出 | 验收 |
|-----|-----|-----|-----|
| 大使等级 + 申请 | 1.5天 | ambassador/level, apply | 样式+功能验证 |
| 功德分 + 积分 + 提现 | 1.5天 | ambassador/merit-points, cash-points, withdraw | 样式+功能验证 |
| 团队 + 二维码 | 1天 | ambassador/team, qrcode | 样式+功能验证 |
| 合同签署 + 活动记录 | 1天 | ambassador/contract-sign, activity-records | 样式+功能验证 |

### 阶段六：辅助页面（2人天）

| 任务 | 时间 | 产出 | 验收 |
|-----|-----|-----|-----|
| 商学院 | 1天 | academy/intro, cases, materials | 样式+功能验证 |
| 商城 + 公告 | 1天 | mall/index, common/announcement | 样式+功能验证 | |

### 阶段七：优化测试（2人天）

- 响应式适配测试
- 性能优化
- Bug 修复

---

## 八、验收标准

### 8.1 样式验收（实现前必须完成）

**实现页面前的必做清单：**

| 步骤 | 操作 | 检查点 |
|-----|------|-------|
| 1 | 阅读原型HTML | `<body>` 背景色、页面容器结构 |
| 2 | 阅读组件CSS | `button.css`, `form.css`, `card.css` 等 |
| 3 | 对比类名 | 原型中使用的 `.t-button`, `.t-card` 等类名 |
| 4 | 检查导航 | 是否需要 `navigationStyle: custom` |
| 5 | 检查颜色 | 原型使用的颜色值（背景、文字、边框） |

**样式验证清单：**

- [ ] 背景颜色与原型一致
- [ ] 按钮颜色（Primary浅蓝填充 vs 纯蓝填充）
- [ ] 卡片圆角、边框、阴影
- [ ] 输入框、选择器样式
- [ ] 顶部导航栏（是否需要自定义、状态栏高度）
- [ ] 字体大小、颜色
- [ ] 间距（padding/margin）

### 8.2 功能验收

| 功能 | 验收标准 |
|-----|---------|
| 微信登录 | 成功获取用户信息，跳转首页 |
| Banner轮播 | 滑动流畅，指示器正确 |
| 课程列表 | 价格、状态显示正确 |
| 支付流程 | 支付成功，订单状态更新 |
| 大使等级 | 根据用户状态显示正确等级（准青鸾→青鸾→鸿鹄→金凤） |

### 8.2 响应式验收

- iPhone SE (320px) ✅
- iPhone 13/14 (375px) ✅
- iPhone Pro Max (414px) ✅
- Android 各尺寸 ✅

### 8.3 性能验收

- 页面加载 < 2秒
- 列表滑动 60fps
- 图片懒加载正常

---

## 九、风险与对策

| 风险 | 影响 | 对策 |
|-----|-----|-----|
| 支付功能 | 无法完成支付 | 使用云函数处理支付 |
| 大使逻辑 | 开发周期延长 | 先完成基础功能 |
| 样式适配 | 多端显示不一致 | 使用响应式CSS |

---

## 十、参考资源

### 10.1 原型图
- 路径：`prototype-tdesign/pages/`
- 启动：`cd prototype-tdesign && python -m http.server 8000`

### 10.2 设计规范
- 颜色变量：`prototype-tdesign/styles/tdesign-theme.css`
- 组件样式：`prototype-tdesign/components/*.css`
- TDesign CSS 类名前缀：`.t-`

### 10.3 原型 HTML 内联样式处理

**注意**：部分原型 HTML 文件包含 `<style>` 标签，需要特殊处理：

#### 10.3.1 设备外壳背景样式（可忽略）

大部分原型页面的 `<style>` 包含：
```css
body {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```
**这是 iPhone 设备外壳的背景样式，小程序不需要复制。**

#### 10.3.2 页面特定样式（需要复制）

| 页面 | 内联样式内容 | 处理方式 |
|-----|-------------|---------|
| `mine/consultation.html` | 聊天消息样式 `.chat-message`, `.chat-bubble` | 复制到全局样式或页面样式 |

**处理流程：**
1. 识别 `<style>` 中的设备背景样式 → **忽略**
2. 识别 `<style>` 中的业务样式 → **复制到项目**
3. 将复制的样式整理到 `src/styles/common.scss` 或页面 scoped 样式中

### 10.4 已实现的登录（cloudbase.ts）
- `signInWithOpenId` - 微信静默登录
- `signInWithPhoneAuth` - 手机号一键登录
- `signInWithPhoneCode` - 手机验证码登录
- `signInWithPassword` - 密码登录
- `signInWithEmailCode` - 邮箱验证码登录

### 10.5 文档
- [UniApp 文档](https://uniapp.dcloud.io/)
- [云开发文档](https://cloud.tencent.com/document/product/876)

---

**计划版本**：v2.0
**预计总工时**：20人天
