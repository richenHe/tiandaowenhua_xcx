# 🧪 天道文化小程序 - 自动化测试完整指南

> **适用对象**：测试人员、开发人员  
> **最后更新**：2026-02-15  
> **文档用途**：后台管理接口测试 + 小程序接口测试

---

## 📚 目录

- [一、测试概念入门](#一测试概念入门)
- [二、测试系统架构](#二测试系统架构)
- [三、测试标准（判定规则）](#三测试标准判定规则)
- [四、快速开始](#四快速开始)
- [五、测试数据管理](#五测试数据管理)
- [六、测试结果分析](#六测试结果分析)
- [七、常见问题处理](#七常见问题处理)
- [八、小程序接口测试指南](#八小程序接口测试指南)

---

## 一、测试概念入门

### 1.1 什么是接口测试？

**接口测试（API Testing）** 是验证系统接口是否按照设计正确工作的测试方法。

#### 📌 与单元测试的区别

| 对比项 | 单元测试 | 接口测试（当前使用） |
|--------|---------|---------------------|
| **测试对象** | 单个函数/方法 | 完整的 HTTP 接口 |
| **测试范围** | 代码的最小单元 | 从请求到响应的完整流程 |
| **依赖环境** | 隔离（使用 Mock） | 真实环境（数据库、云函数） |
| **执行速度** | 极快（毫秒级） | 较慢（秒级） |
| **测试价值** | 验证代码逻辑 | 验证业务功能 |

**示例对比**：

```javascript
// ❌ 单元测试例子（不是我们使用的）
test('计算总价', () => {
  expect(calculateTotal(100, 0.9)).toBe(90);
});

// ✅ 接口测试例子（我们使用的）
{
  name: '获取订单列表',
  method: 'GET',
  url: '/api/admin/order/getOrderList',
  expectedFields: ['total', 'list']  // 验证真实接口返回
}
```

### 1.2 测试系统类型定位

**本测试系统 = 接口测试 + 集成测试**

- ✅ 测试完整的 API 端到端流程
- ✅ 使用真实的云开发环境
- ✅ 验证数据库操作是否正确
- ✅ 检查业务逻辑完整性

---

## 二、测试系统架构

### 2.1 测试覆盖范围

本系统共覆盖 **63 个后台管理接口**：

```
📦 用户管理模块    4 个接口   ✅ 学员列表、详情、推荐人管理
📦 订单管理模块    4 个接口   ✅ 订单列表、退款、提现审核
📦 课程管理模块   20 个接口   ✅ 课程、排期、预约、案例、资料
📦 大使管理模块   14 个接口   ✅ 大使、申请、活动、合约管理
📦 系统管理模块   21 个接口   ✅ 统计、管理员、配置、公告、通知、反馈
────────────────────────────────────────
总计：           63 个接口
```

### 2.2 技术架构

```
┌─────────────────────────────────────┐
│  测试工具（admin/api-test.html）      │
│  - 自动登录获取 Token                 │
│  - 按顺序执行测试用例                 │
│  - 验证返回数据格式                   │
└──────────────┬──────────────────────┘
               │ HTTP POST
               ▼
┌─────────────────────────────────────┐
│  腾讯云开发 CloudBase                 │
│  cloud1-0gnn3mn17b581124             │
│  - 云函数：user/order/course/...     │
│  - 身份认证：JWT Token               │
└──────────────┬──────────────────────┘
               │ SQL
               ▼
┌─────────────────────────────────────┐
│  MySQL 数据库                         │
│  tiandao_culture                     │
│  - users, orders, courses...         │
└─────────────────────────────────────┘
```

---

## 三、测试标准（判定规则）

### 3.1 四层验证机制

每个接口测试会经过 **4 个层次** 的验证，任何一层失败则测试失败：

#### ✅ Level 1：HTTP 状态码检查

```javascript
// 必须返回 200 OK
if (!response.ok) {
  ❌ 测试失败
}
```

**失败原因**：
- 404 Not Found - 接口地址错误
- 500 Internal Server Error - 服务器内部错误
- 网络连接失败

---

#### ✅ Level 2：业务成功标识

```javascript
// 返回数据必须包含 success: true
if (!result.success) {
  ❌ 测试失败
}
```

**失败原因**：
- `code: 401` - Token 过期或无效
- `code: 400` - 参数错误
- 业务逻辑错误（数据不存在、权限不足等）

---

#### ✅ Level 3：必需字段完整性

```javascript
// 检查预定义的必需字段
expectedFields: ['total', 'list']

// 所有字段必须存在
const allFieldsExist = expectedFields.every(field => 
  response.data[field] !== undefined
);

if (!allFieldsExist) {
  ❌ 测试失败: "缺少必需字段: total"
}
```

**测试用例示例**：

```javascript
{
  name: '获取课程列表',
  expectedFields: ['total', 'list'],        // 必须有这 2 个字段
  itemFields: ['id', 'name', 'type']        // list 中每项必须有这 3 个字段
}
```

---

#### ✅ Level 4：数据类型验证

```javascript
// 验证字段类型
fieldChecks: [
  {
    field: 'total',
    exists: true,
    type: 'number'  // ✅ 类型正确
  }
]
```

---

### 3.2 测试通过标准

**所有 4 层验证都通过 = ✅ 测试成功**

```
HTTP 200 ✅
  ↓
success: true ✅
  ↓
必需字段完整 ✅
  ↓
数据类型正确 ✅
  ↓
🎉 测试通过
```

---

## 四、快速开始

### 4.1 环境准备

#### 必需条件：
1. ✅ Chrome 浏览器
2. ✅ 本地服务器（VS Code Live Server 或 Python http.server）
3. ✅ 管理员账号权限

#### 测试环境信息：
```javascript
环境 ID：cloud1-0gnn3mn17b581124
数据库：tiandao_culture
管理员账号：herichen
管理员密码：tiandaowenhua
```

---

### 4.2 启动测试

#### 步骤 1：启动本地服务器

**方式 A：VS Code Live Server**
```
右键点击 admin/api-test.html → Open with Live Server
```

**方式 B：Python 服务器**
```bash
cd d:\project\cursor\work\xcx\admin
python -m http.server 8000

# 访问：http://localhost:8000/api-test.html
```

---

#### 步骤 2：打开测试页面

浏览器访问：
```
http://127.0.0.1:5500/admin/api-test.html
```

页面会 **自动执行**：
1. 使用管理员账号登录
2. 获取并保存 JWT Token
3. 显示 63 个测试模块

---

#### 步骤 3：运行测试

**选项 A：运行所有测试**
```
点击 [运行所有测试] 按钮
→ 自动执行 63 个接口测试
→ 预计耗时 10-20 秒
```

**选项 B：测试单个模块**
```
点击模块卡片（如"用户管理"）
→ 仅测试该模块的接口
```

**选项 C：测试单个接口**
```
在结果列表中点击 [重试] 按钮
→ 重新测试单个接口
```

---

#### 步骤 4：查看结果

测试完成后会显示：

```
┌────────────────────────────────┐
│  总测试数：63                   │
│  ✅ 通过：60 (95.24%)           │
│  ❌ 失败：3                     │
│  ⏱ 耗时：12.5 秒                │
└────────────────────────────────┘
```

点击 **[导出 JSON 报告]** 保存详细结果到 `admin/1.md`

---

### 4.3 测试模式

#### 🔵 真实数据模式（默认）

```
Mock 模式：关闭
```

- ✅ 使用真实的业务数据
- ✅ 测试结果最准确
- ⚠️ 慎重使用删除接口

---

#### 🟠 Mock 模式（调试用）

```
Mock 模式：开启
```

- 使用假数据（userId=1, orderId=999）
- 适合调试接口连通性
- ⚠️ 可能返回"数据不存在"错误

---

## 五、测试数据管理

### 5.1 数据添加规范 ⭐

遵循 **优先级原则**：

#### 🥇 优先：通过接口创建

```javascript
// ✅ 好的做法：测试创建接口的同时添加数据
{
  name: '创建课程',
  method: 'POST',
  body: { 
    name: '测试课程',
    type: 1,
    current_price: 1688 
  }
}
```

**适用场景**：
- ✅ 课程、排期、案例、资料
- ✅ 大使活动、公告、配置
- ✅ 所有有"创建接口"的数据

---

#### 🥈 特殊情况：直接插入数据库

```sql
-- 仅在以下情况使用 SQL 插入
INSERT INTO tiandao_culture.appointments (user_id, course_id, ...)
VALUES (30, 1, ...);
```

**适用场景**：
- 无管理端创建接口（如 `appointments`）
- 依赖复杂业务逻辑（如 `user_courses` 需要支付流程）
- 关联关系数据（预约需要关联用户课程）

---

### 5.2 测试数据标记规范

**所有测试数据必须明确标识**：

```javascript
// ✅ 正确示例
{
  name: '测试课程',           // 名称以"测试"开头
  order_no: 'TEST20260215001', // 订单号带 TEST 前缀
  user_id: 30,                // 使用固定测试用户
  real_name: '测试用户'
}

// ❌ 错误示例
{
  name: '天道文化课程',        // 看起来像真实数据
  user_id: 50                 // 使用随机用户
}
```

**标记规则**：
1. 名称字段：`测试XXX` 或 `Test XXX`
2. 订单号：`TEST` 前缀
3. 用户 ID：固定使用 `30`（测试专用用户）
4. 状态：可以是任何测试需要的状态

---

### 5.3 数据清理策略

**❌ 不需要清理测试数据**

理由：
- 保留数据用于下次测试
- 避免重复创建浪费时间
- 测试数据有明确标识，不会混淆

**例外情况**：
- 测试删除接口时，允许删除测试数据
- 数据库空间不足时，可手动清理

---

## 六、测试结果分析

### 6.1 理解测试报告

#### 成功的测试 ✅

```json
{
  "name": "1. 获取学员列表",
  "status": "success",
  "method": "GET",
  "url": "/api/admin/user/getUserList",
  "duration": 245,
  "fieldChecks": [
    { "field": "total", "exists": true, "type": "number" },
    { "field": "list", "exists": true, "type": "object" }
  ],
  "error": null
}
```

**解读**：
- ✅ HTTP 请求成功
- ✅ 接口返回成功
- ✅ 所有必需字段存在
- ✅ 数据类型正确
- ⏱ 耗时 245 毫秒

---

#### 失败的测试 ❌

```json
{
  "name": "6. 删除管理员",
  "status": "failed",
  "duration": 226,
  "error": "管理员不存在",
  "missingFields": []
}
```

**解读**：
- ❌ 业务逻辑返回失败
- 原因：id=999 的管理员不存在
- 这是 **预期行为**（测试删除不存在的数据）

---

#### 字段缺失错误 ⚠️

```json
{
  "name": "8. 更新配置",
  "status": "failed",
  "error": "缺少必需字段: success",
  "missingFields": ["success"]
}
```

**解读**：
- ❌ 接口返回数据不完整
- 需要修复：接口应返回 `{ success: true, ... }`
- **这是代码 Bug**，需要开发修复

---

### 6.2 常见失败原因分类

#### 🟢 正常失败（预期行为）

```
❌ "管理员不存在"     → 测试删除 id=999
❌ "订单不存在"       → 测试删除 id=999
❌ "公告不存在"       → 测试删除 id=999
```

✅ **不需要修复** - 这些是故意测试不存在的数据

---

#### 🟡 数据缺失（需要添加测试数据）

```
❌ "配置不存在"       → 需要插入配置数据
❌ "反馈不存在"       → 需要创建反馈记录
❌ "预约记录为空"     → 需要添加预约数据
```

✅ **解决方案**：按照 [五、测试数据管理](#五测试数据管理) 添加数据

---

#### 🔴 代码错误（需要修复接口）

```
❌ "缺少必需字段: success"
❌ "Unknown column 'value_type'"
❌ "缺少必要参数: real_name"
❌ "未知的操作: getConfigList"
```

✅ **解决方案**：
1. 查看云函数日志定位错误
2. 修复代码（字段名、参数、handler 注册）
3. 部署云函数
4. 重新测试

---

#### 🟣 环境问题（需要报告）

```
❌ "invalid wx openapi access_token"
❌ "微信支付 API 调用失败"
❌ "网络连接超时"
```

⚠️ **重要**：不要自动跳过或添加测试模式，应该：
1. 记录问题详情
2. 报告给负责人
3. 等待外部依赖修复

---

### 6.3 使用云函数日志调试

#### 步骤 1：获取 RequestId

从测试报告或浏览器控制台找到失败请求的 RequestId：

```javascript
console.log('[updateConfig] RequestId: e9cd4556-0a7c-11f1-a9e0-525400655719');
```

---

#### 步骤 2：查询日志

使用 CloudBase MCP 工具：

```javascript
// 查询最近的日志
getFunctionLogs({
  name: 'system',
  startTime: '2026-02-15 22:00:00',
  endTime: '2026-02-15 23:00:00'
});

// 查询详细日志
getFunctionLogDetail({
  requestId: 'e9cd4556-0a7c-11f1-a9e0-525400655719'
});
```

---

#### 步骤 3：分析日志

日志会显示：
- 请求参数
- 执行步骤
- 数据库查询
- 错误堆栈

**示例**：

```
[admin:updateConfig] 管理员 5 更新配置: system.maintenance
[DB] insert error: {
  "message": "column value_type not found in table system_configs"
}
```

👉 **分析**：字段名错误，应该是 `config_type` 而不是 `value_type`

---

## 七、常见问题处理

### 7.1 Token 过期

**现象**：
```
❌ 登录已过期
❌ code: 401
```

**解决方案**：
```
1. 测试工具会自动重新登录
2. 如果自动登录失败，点击 [手动登录] 按钮
3. 检查管理员账号密码是否正确
```

---

### 7.2 数据库连接错误

**现象**：
```
❌ "Unknown database 'cloud1-0gnn3mn17b581124'"
```

**原因**：使用了错误的数据库名

**解决方案**：
```sql
-- ✅ 正确：使用业务数据库
SELECT * FROM tiandao_culture.users;

-- ❌ 错误：使用环境 ID 作为数据库名
SELECT * FROM cloud1-0gnn3mn17b581124.users;
```

---

### 7.3 接口返回格式错误

**现象**：
```json
{
  "body": "{\"success\":true,...}"  // 字符串格式
}
```

**原因**：CloudBase HTTP Access Service 返回格式

**解决方案**：测试工具会自动解析 `body` 字段

```javascript
// 自动处理
if (result.body && typeof result.body === 'string') {
  result = JSON.parse(result.body);
}
```

---

### 7.4 字段名不一致

**现象**：
```
❌ "缺少必需字段: course_id"
```

**原因**：接口返回 `id`，测试期望 `course_id`

**解决方案 A**：修改接口返回
```javascript
// ✅ 返回明确的字段名
return response.success({ course_id: id }, '创建成功');
```

**解决方案 B**：修改测试用例
```javascript
// ✅ 期望接口实际返回的字段
expectedFields: ['id']  // 而不是 ['course_id']
```

---

### 7.5 外部依赖问题

**现象**：
```
❌ "invalid wx openapi access_token"
```

**⚠️ 重要规则**：

```
❌ 不要修改代码跳过这个错误
❌ 不要添加测试模式
✅ 记录问题并报告
```

**原因**：
- 微信支付 API 需要真实的 access_token
- 测试环境无法获取线上 token
- 这是外部依赖问题，不是代码错误

---

## 八、小程序接口测试指南

### 8.1 小程序接口 vs 后台接口

#### 主要区别

| 对比项 | 后台接口 | 小程序接口 |
|--------|---------|-----------|
| **认证方式** | JWT Token | OpenID（微信登录） |
| **调用方式** | HTTP POST | CloudBase SDK |
| **权限控制** | 管理员角色 | 用户身份 |
| **测试难度** | 简单 | 较复杂（需要模拟登录） |

---

### 8.2 创建小程序测试文件

#### 步骤 1：复制测试模板

```bash
# 复制后台测试文件作为模板
cp admin/api-test.html admin/miniprogram-api-test.html
```

---

#### 步骤 2：修改认证方式

```javascript
// ❌ 后台接口认证（JWT Token）
const requestData = {
  action: 'getUserInfo',
  jwtToken: adminToken,  // 管理员 Token
  ...params
};

// ✅ 小程序接口认证（OpenID）
const requestData = {
  action: 'getUserInfo',
  openid: testUserOpenid,  // 用户 OpenID
  ...params
};
```

---

#### 步骤 3：配置测试用户

```javascript
// 小程序测试配置
const testConfig = {
  // 测试用户信息
  testUser: {
    openid: 'oTest-testuser-openid-123456',
    userId: 30,
    nickname: '测试用户'
  },
  
  // 云函数地址
  baseUrl: 'https://cloud1-0gnn3mn17b581124.service.tcloudbase.com'
};
```

---

### 8.3 小程序测试用例示例

#### 用户模块

```javascript
{
  id: 'user',
  name: '用户模块',
  description: '登录、个人信息、推荐人（6个接口）',
  tests: [
    {
      name: '1. 微信登录',
      method: 'POST',
      url: '/user',
      body: { 
        action: 'login',
        code: 'test_wx_code_123',  // 模拟微信授权码
        userInfo: {
          nickName: '测试用户',
          avatarUrl: 'https://example.com/avatar.png'
        }
      },
      expectedFields: ['openid', 'session_key', 'user']
    },
    {
      name: '2. 获取个人信息',
      method: 'POST',
      url: '/user',
      body: { 
        action: 'getUserInfo',
        openid: 'oTest-testuser-openid-123456'
      },
      expectedFields: ['user', 'statistics']
    },
    {
      name: '3. 更新个人资料',
      method: 'POST',
      url: '/user',
      body: { 
        action: 'updateUserInfo',
        openid: 'oTest-testuser-openid-123456',
        real_name: '张三',
        phone: '13800138000'
      },
      expectedFields: ['success']
    }
  ]
}
```

---

#### 课程模块

```javascript
{
  id: 'course',
  name: '课程模块',
  description: '课程列表、详情、预约（8个接口）',
  tests: [
    {
      name: '1. 获取课程列表',
      method: 'POST',
      url: '/course',
      body: { 
        action: 'getCourseList',
        page: 1,
        page_size: 20,
        type: 1  // 课程类型
      },
      expectedFields: ['total', 'list'],
      itemFields: ['id', 'name', 'current_price', 'cover_image']
    },
    {
      name: '2. 获取课程详情',
      method: 'POST',
      url: '/course',
      body: { 
        action: 'getCourseDetail',
        openid: 'oTest-testuser-openid-123456',
        course_id: 1
      },
      expectedFields: ['course', 'schedules', 'materials', 'cases']
    },
    {
      name: '3. 预约课程',
      method: 'POST',
      url: '/course',
      body: { 
        action: 'createAppointment',
        openid: 'oTest-testuser-openid-123456',
        course_id: 1,
        class_record_id: 1,
        contact_name: '张三',
        contact_phone: '13800138000'
      },
      expectedFields: ['appointment_id']
    }
  ]
}
```

---

#### 订单模块

```javascript
{
  id: 'order',
  name: '订单模块',
  description: '下单、支付、订单列表（7个接口）',
  tests: [
    {
      name: '1. 创建订单',
      method: 'POST',
      url: '/order',
      body: { 
        action: 'createOrder',
        openid: 'oTest-testuser-openid-123456',
        course_id: 1,
        payment_type: 1,  // 纯现金
        total_amount: 1688
      },
      expectedFields: ['order_id', 'order_no']
    },
    {
      name: '2. 获取支付参数',
      method: 'POST',
      url: '/order',
      body: { 
        action: 'createPayment',
        openid: 'oTest-testuser-openid-123456',
        order_id: 1
      },
      expectedFields: ['payment_params']
    },
    {
      name: '3. 我的订单列表',
      method: 'POST',
      url: '/order',
      body: { 
        action: 'getMyOrders',
        openid: 'oTest-testuser-openid-123456',
        page: 1,
        page_size: 20
      },
      expectedFields: ['total', 'list'],
      itemFields: ['order_id', 'order_no', 'status', 'total_amount']
    }
  ]
}
```

---

### 8.4 小程序测试注意事项

#### ⚠️ 关键差异

1. **不需要 JWT Token**
   ```javascript
   // ❌ 不要使用
   jwtToken: 'xxx'
   
   // ✅ 使用 OpenID
   openid: 'oTest-xxx'
   ```

2. **云函数路径不同**
   ```javascript
   // 后台接口
   url: '/api/admin/user/getUserList'
   function: 'user'
   action: 'getUserList'
   
   // 小程序接口
   url: '/user'  // 直接使用云函数名
   action: 'getUserInfo'
   ```

3. **参数格式差异**
   ```javascript
   // 后台接口（驼峰命名）
   { userId: 30, pageSize: 20 }
   
   // 小程序接口（下划线命名）
   { user_id: 30, page_size: 20 }
   ```

4. **测试数据隔离**
   ```javascript
   // 使用专门的测试用户
   testUser: {
     openid: 'oTest-testuser-openid-123456',
     userId: 30,  // 固定测试用户 ID
     nickname: '测试用户'
   }
   ```

---

### 8.5 小程序测试流程

```
1. 模拟微信登录
   ↓
2. 获取测试用户 OpenID
   ↓
3. 使用 OpenID 调用各个接口
   ↓
4. 验证返回数据格式
   ↓
5. 检查业务逻辑正确性
```

---

## 九、测试最佳实践

### 9.1 测试前检查清单

```
□ 确认使用正确的数据库（tiandao_culture）
□ 管理员账号密码正确
□ 云函数最新代码已部署
□ 测试数据已准备（参考第五章）
□ 了解预期的失败测试（删除不存在的数据）
```

---

### 9.2 测试执行建议

1. **首次测试**
   - 先运行单个模块测试
   - 逐个验证接口
   - 发现问题及时修复

2. **回归测试**
   - 修改代码后运行所有测试
   - 确保没有影响现有功能

3. **定期测试**
   - 每天执行一次完整测试
   - 保持接口稳定性

---

### 9.3 测试报告管理

```
测试报告命名规范：
- admin/1.md              ← 最新测试结果
- admin/test-20260215.md  ← 历史记录（按日期）
```

---

### 9.4 错误修复流程

```
发现失败 → 分类原因 → 选择方案 → 修复验证
   ↓          ↓          ↓          ↓
测试结果   数据/代码   添加/修复   重新测试
          /环境              ↓
                          ✅通过
```

---

## 十、附录

### 10.1 测试配置文件

```javascript
// admin/api-test.html 配置项
const CONFIG = {
  ENV_ID: 'cloud1-0gnn3mn17b581124',
  
  CLOUD_FUNCTIONS: {
    USER: 'user',
    ORDER: 'order',
    COURSE: 'course',
    AMBASSADOR: 'ambassador',
    SYSTEM: 'system'
  },
  
  ADMIN: {
    USERNAME: 'herichen',
    PASSWORD: 'tiandaowenhua'
  },
  
  DATABASE: 'tiandao_culture',
  
  TEST_USER_ID: 30  // 固定测试用户
};
```

---

### 10.2 云函数列表

| 云函数名 | 模块 | 接口数量 |
|---------|------|---------|
| `user` | 用户管理 | 16 |
| `order` | 订单管理 | 12 |
| `course` | 课程管理 | 20 |
| `ambassador` | 大使管理 | 14 |
| `system` | 系统管理 | 21 |

---

### 10.3 数据库表清单

#### 核心业务表

```
users                用户表
orders               订单表
courses              课程表
class_records        排期表
appointments         预约表
user_courses         用户课程关系表
ambassador_users     大使信息表
```

#### 系统管理表

```
admin_users          管理员表
system_configs       系统配置表
announcements        公告表
feedbacks            反馈表
notification_logs    通知日志表
```

---

### 10.4 快速参考

#### 测试 URL

```
后台测试：http://127.0.0.1:5500/admin/api-test.html
小程序测试：http://127.0.0.1:5500/admin/miniprogram-api-test.html
```

#### 常用命令

```bash
# 启动本地服务器
python -m http.server 8000

# 查看云函数日志
# 使用 CloudBase MCP 工具

# 查询数据库
# 使用 executeReadOnlySQL 工具
```

---

## 📞 支持与反馈

**遇到问题？**
1. 查看本文档相关章节
2. 检查云函数日志
3. 查看浏览器控制台
4. 联系开发团队

**文档更新**  
最后更新：2026-02-15  
版本：v2.0

---

## 🎯 下一步

现在你已经掌握：
- ✅ 接口测试的基本概念
- ✅ 测试系统的使用方法
- ✅ 测试结果的分析能力
- ✅ 常见问题的解决方案
- ✅ 小程序接口测试指南

**准备好测试小程序接口了吗？** 🚀

按照第八章的指南创建小程序测试文件，开始你的测试之旅！
