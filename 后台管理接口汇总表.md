# 天道文化 - 后台管理接口汇总表

> **更新日期**: 2026-02-12  
> **接口总数**: 80+  
> **云函数数量**: 5 个（user, order, course, ambassador, system）

---

## 📊 接口统计

| 模块 | 云函数名 | 管理端接口数 | 说明 |
|-----|---------|------------|------|
| 用户管理 | user | 4 | 学员信息、推荐关系管理 |
| 订单管理 | order | 4 | 订单、退款、提现审核 |
| 课程管理 | course | 20 | 课程、排期、预约、案例、资料 |
| 大使管理 | ambassador | 15 | 大使、申请、活动、合约 |
| 系统管理 | system | 21 | 统计、管理员、配置、公告、反馈 |
| **合计** | **5** | **64** | - |

---

## 1️⃣ 用户管理模块 (user)

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 1.1 | `getUserList` | 获取学员列表 | page, pageSize, keyword, ambassadorLevel | {total, list} 用户列表 |
| 1.2 | `getUserDetail` | 获取学员详情 | userId | 用户信息、课程、订单、积分统计 |
| 1.3 | `updateUserReferee` | 修改学员推荐人 | userId, newRefereeId, reason | 修改结果 |
| 1.4 | `getRefereeChangeLogs` | 推荐人变更记录 | userId, page, pageSize | {total, list} 变更记录 |

**调用示例**：
```javascript
// 获取学员列表
await app.callFunction({
  name: 'user',
  data: {
    action: 'getUserList',
    jwtToken: token,
    page: 1,
    pageSize: 20
  }
});
```

---

## 2️⃣ 订单管理模块 (order)

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 2.1 | `getOrderList` | 获取订单列表 | page, page_size, pay_status, keyword | {total, list} 订单列表 |
| 2.2 | `getOrderDetail` | 获取订单详情 | order_no | 订单完整信息 |
| 2.3 | `refund` | 订单退款 | order_no, refund_amount, refund_reason | 退款结果 |
| 2.4 | `withdrawAudit` | 提现审核 | withdrawal_id, status, reject_reason | 审核结果 |

**调用示例**：
```javascript
// 审核提现
await app.callFunction({
  name: 'order',
  data: {
    action: 'withdrawAudit',
    jwtToken: token,
    withdrawal_id: 123,
    status: 1  // 1=通过，2=拒绝
  }
});
```

---

## 3️⃣ 课程管理模块 (course)

### 3.1 课程管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.1.1 | `getCourseList` | 获取课程列表 | page, page_size, type, status | {total, list} 课程列表 |
| 3.1.2 | `createCourse` | 创建课程 | name, type, current_price | {course_id} 课程ID |
| 3.1.3 | `updateCourse` | 更新课程 | id, ...更新字段 | 更新结果 |
| 3.1.4 | `deleteCourse` | 删除课程 | id | 删除结果 |

### 3.2 排期管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.2.1 | `getClassRecordList` | 获取排期列表 | page, page_size, course_id, status | {total, list} 排期列表 |
| 3.2.2 | `createClassRecord` | 创建排期 | course_id, class_date, start_time | {class_record_id} |
| 3.2.3 | `updateClassRecord` | 更新排期 | id, ...更新字段 | 更新结果 |
| 3.2.4 | `deleteClassRecord` | 删除排期 | id | 删除结果 |

### 3.3 预约管理（3个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.3.1 | `getAppointmentList` | 获取预约列表 | page, page_size, course_id, status | {total, list} 预约列表 |
| 3.3.2 | `updateAppointmentStatus` | 更新预约状态 | appointment_id, status | 更新结果 |
| 3.3.3 | `batchCheckin` | 批量签到 | appointment_ids | {success_count, failed_count} |

### 3.4 案例管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.4.1 | `getCaseList` | 获取案例列表 | page, page_size, keyword | {total, list} 案例列表 |
| 3.4.2 | `createCase` | 创建案例 | ... | {case_id} |
| 3.4.3 | `updateCase` | 更新案例 | id, ... | 更新结果 |
| 3.4.4 | `deleteCase` | 删除案例 | id | 删除结果 |

### 3.5 资料管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.5.1 | `getMaterialList` | 获取资料列表 | page, page_size, keyword | {total, list} 资料列表 |
| 3.5.2 | `createMaterial` | 创建资料 | ... | {material_id} |
| 3.5.3 | `updateMaterial` | 更新资料 | id, ... | 更新结果 |
| 3.5.4 | `deleteMaterial` | 删除资料 | id | 删除结果 |

### 3.6 学院内容管理（1个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 3.6.1 | `manageAcademyContent` | 管理学院内容 | ... | 管理结果 |

**调用示例**：
```javascript
// 批量签到
await app.callFunction({
  name: 'course',
  data: {
    action: 'batchCheckin',
    jwtToken: token,
    appointment_ids: [101, 102, 103]
  }
});
```

---

## 4️⃣ 大使管理模块 (ambassador)

### 4.1 大使管理（2个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 4.1.1 | `getAmbassadorList` | 获取大使列表 | page, page_size, level, keyword | {total, list} 大使列表 |
| 4.1.2 | `getAmbassadorDetail` | 获取大使详情 | ambassador_id | 大使详细信息 |

### 4.2 申请管理（2个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 4.2.1 | `getApplicationList` | 获取申请列表 | page, page_size, status | {total, list} 申请列表 |
| 4.2.2 | `auditApplication` | 审核申请 | application_id, approved, reject_reason | 审核结果 |

### 4.3 活动管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 4.3.1 | `getActivityList` | 获取活动列表 | page, page_size | {total, list} 活动列表 |
| 4.3.2 | `createActivity` | 创建活动 | ... | {activity_id} |
| 4.3.3 | `updateActivity` | 更新活动 | id, ... | 更新结果 |
| 4.3.4 | `deleteActivity` | 删除活动 | id | 删除结果 |

### 4.4 合约管理（7个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 4.4.1 | `getContractTemplateList` | 获取合约模板列表 | page, page_size | {total, list} 模板列表 |
| 4.4.2 | `createContractTemplate` | 创建合约模板 | ... | {template_id} |
| 4.4.3 | `updateContractTemplate` | 更新合约模板 | id, ... | 更新结果 |
| 4.4.4 | `deleteContractTemplate` | 删除合约模板 | id | 删除结果 |
| 4.4.5 | `getSignatureList` | 获取签约列表 | page, page_size | {total, list} 签约列表 |
| 4.4.6 | `getContractVersions` | 获取合约版本 | template_id | {list} 版本列表 |
| 4.4.7 | `getExpiringContracts` | 获取即将到期合约 | days | {list} 合约列表 |

**调用示例**：
```javascript
// 审核大使申请
await app.callFunction({
  name: 'ambassador',
  data: {
    action: 'auditApplication',
    jwtToken: token,
    application_id: 123,
    approved: true
  }
});
```

---

## 5️⃣ 系统管理模块 (system)

### 5.1 认证与统计（2个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.1.1 | `login` | 管理员登录 | username, password | {token, admin} |
| 5.1.2 | `getStatistics` | 获取统计数据 | 无 | 仪表盘统计数据 |

### 5.2 管理员管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.2.1 | `getAdminUserList` | 获取管理员列表 | page, page_size, status | {total, list} 管理员列表 |
| 5.2.2 | `createAdminUser` | 创建管理员 | username, password, role | {admin_id} |
| 5.2.3 | `updateAdminUser` | 更新管理员 | id, ... | 更新结果 |
| 5.2.4 | `deleteAdminUser` | 删除管理员 | id | 删除结果 |

### 5.3 系统配置（2个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.3.1 | `getConfig` | 获取系统配置 | config_key | 配置信息 |
| 5.3.2 | `updateConfig` | 更新系统配置 | config_key, config_value | 更新结果 |

### 5.4 公告管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.4.1 | `getAnnouncementList` | 获取公告列表 | page, page_size, status | {total, list} 公告列表 |
| 5.4.2 | `createAnnouncement` | 创建公告 | title, content, type | {announcement_id} |
| 5.4.3 | `updateAnnouncement` | 更新公告 | id, ... | 更新结果 |
| 5.4.4 | `deleteAnnouncement` | 删除公告 | id | 删除结果 |

### 5.5 反馈管理（2个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.5.1 | `getFeedbackList` | 获取反馈列表 | page, page_size, type, status | {total, list} 反馈列表 |
| 5.5.2 | `replyFeedback` | 回复反馈 | feedback_id, reply_content | 回复结果 |

### 5.6 通知管理（4个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.6.1 | `getNotificationConfigList` | 获取通知配置列表 | page, page_size | {total, list} 配置列表 |
| 5.6.2 | `createNotificationConfig` | 创建通知配置 | ... | {config_id} |
| 5.6.3 | `updateNotificationConfig` | 更新通知配置 | id, ... | 更新结果 |
| 5.6.4 | `getNotificationLogs` | 获取通知日志 | page, page_size, start_date | {total, list} 日志列表 |
| 5.6.5 | `sendNotification` | 发送通知 | user_ids, template_id, data | 发送结果 |

### 5.7 大使等级配置（3个接口）

| 序号 | Action | 说明 | 关键参数 | 返回数据 |
|-----|--------|------|---------|---------|
| 5.7.1 | `getAmbassadorLevelConfigs` | 获取等级配置 | 无 | {list} 配置列表 |
| 5.7.2 | `initAmbassadorLevelConfigs` | 初始化等级配置 | 无 | 初始化结果 |
| 5.7.3 | `updateAmbassadorLevelConfig` | 更新等级配置 | level, ... | 更新结果 |

**调用示例**：
```javascript
// 获取统计数据
await app.callFunction({
  name: 'system',
  data: {
    action: 'getStatistics',
    jwtToken: token
  }
});
```

---

## 📌 分类索引

### 按功能分类

#### 🔍 查询类接口（26个）
- `getUserList`, `getUserDetail`, `getOrderList`, `getOrderDetail`
- `getCourseList`, `getClassRecordList`, `getAppointmentList`
- `getCaseList`, `getMaterialList`
- `getAmbassadorList`, `getAmbassadorDetail`, `getApplicationList`
- `getActivityList`, `getContractTemplateList`, `getSignatureList`
- `getContractVersions`, `getExpiringContracts`
- `getStatistics`, `getAdminUserList`, `getConfig`
- `getAnnouncementList`, `getFeedbackList`
- `getNotificationConfigList`, `getNotificationLogs`
- `getAmbassadorLevelConfigs`, `getRefereeChangeLogs`

#### ➕ 创建类接口（10个）
- `createCourse`, `createClassRecord`, `createCase`, `createMaterial`
- `createActivity`, `createContractTemplate`
- `createAdminUser`, `createAnnouncement`, `createNotificationConfig`

#### 🔄 更新类接口（11个）
- `updateUserReferee`, `updateCourse`, `updateClassRecord`
- `updateAppointmentStatus`, `updateCase`, `updateMaterial`
- `updateActivity`, `updateContractTemplate`
- `updateAdminUser`, `updateConfig`, `updateAnnouncement`
- `updateNotificationConfig`, `updateAmbassadorLevelConfig`

#### 🗑️ 删除类接口（10个）
- `deleteCourse`, `deleteClassRecord`, `deleteCase`, `deleteMaterial`
- `deleteActivity`, `deleteContractTemplate`
- `deleteAdminUser`, `deleteAnnouncement`

#### ✅ 审核类接口（2个）
- `auditApplication`, `withdrawAudit`

#### 📦 批量操作接口（1个）
- `batchCheckin`

#### 🔐 认证类接口（1个）
- `login`

---

## 🔗 快速导航

- [完整 API 文档](./后台管理接口文档.md) - 详细的接口说明和参数
- [API 快速参考](./后台管理API快速参考.md) - 常用示例和代码模板
- [项目开发规范](./项目开发规范.md) - 开发规范和最佳实践

---

## 📊 使用统计

### 最常用接口（Top 10）

| 排名 | 接口 | 模块 | 使用场景 |
|-----|------|------|---------|
| 1 | `login` | system | 管理员登录 |
| 2 | `getStatistics` | system | 仪表盘统计 |
| 3 | `getUserList` | user | 学员列表查看 |
| 4 | `getOrderList` | order | 订单列表查看 |
| 5 | `getCourseList` | course | 课程列表管理 |
| 6 | `getAppointmentList` | course | 预约管理 |
| 7 | `withdrawAudit` | order | 提现审核 |
| 8 | `auditApplication` | ambassador | 大使申请审核 |
| 9 | `getAmbassadorList` | ambassador | 大使列表查看 |
| 10 | `batchCheckin` | course | 批量签到 |

---

## 📝 更新日志

### v1.0 (2026-02-12)
- ✅ 完成所有管理端接口汇总
- ✅ 按模块、功能分类整理
- ✅ 添加快速查询索引

---

**文档维护**: 天道文化技术团队  
**最后更新**: 2026-02-12





